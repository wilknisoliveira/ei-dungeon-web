services:
  web:
    image: ei-dungeon-web:latest
    build:
      context: .
      args:
        SITE_URL: ${SITE_URL:-http://localhost:8000}
    ports:
      - "${APP_PORT:-8000}:80"
    restart: unless-stopped
